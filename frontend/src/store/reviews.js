import { csrfFetch } from './csrf';


const SPOT_REVIEWS = '/reviews/spot'







export const ReviewsReducer = (state = {}, action) => {
    let newState;
    switch (action.type) {
        case SPOT_REVIEWS:
        
        default:
            return state
    }
}
